# Synthetic automated data

Only deterministic synthetic records belong here. Names use `Test`, identifiers use `TEST-*`, and email addresses use `example.invalid`. These files are fixtures/staging inputs; they are not evidence that every v1.8 import endpoint is implemented.
