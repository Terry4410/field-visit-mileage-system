# Master-data import templates

These CSV files define repository-safe staging contracts for the required master-data categories. They contain headers only. Real Final UAT values remain outside Git and must use the approved import preview/validate/confirm path. Template-to-endpoint mapping must be finalized with the corresponding Epic; do not import these files directly until that mapping is tested.
