import {useState} from "react";import {useAuth} from "../auth";
export default function LoginPage(){const {login}=useAuth();const [account,setAccount]=useState("visitor01"),[password,setPassword]=useState("123456"),[error,setError]=useState(""),[busy,setBusy]=useState(false);
 const go=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError("");try{await login(account,password)}catch(x){setError(x instanceof Error?x.message:"登入失敗")}finally{setBusy(false)}};
 return <div className="login-bg"><form className="login" onSubmit={go}><h1>外訪行程與里程管理</h1><p>Azure SQL 多人 UAT｜Schema 1.5.0</p><label>帳號（EmployeeNo）<input value={account} onChange={e=>setAccount(e.target.value)}/></label><label>密碼<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="alert danger">{error}</div>}<button className="btn full" disabled={busy}>{busy?"登入中…":"登入"}</button><div className="demo">{["visitor01","leader01","admin01","gov01"].map(x=><button type="button" key={x} onClick={()=>setAccount(x)}>{x}</button>)}</div><div className="alert">UAT Demo Password 由 Azure App Service 設定；正式環境改 Microsoft Entra ID。</div></form></div>}
