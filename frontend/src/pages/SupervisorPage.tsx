import {useEffect,useState} from "react";import {api} from "../api";import type {MileageReport} from "../types";import {downloadCsv,money} from "../utils";
export default function SupervisorPage(){const [rows,setRows]=useState<MileageReport[]>([]),[start,setStart]=useState("2026-01-01"),[end,setEnd]=useState(new Date().toISOString().slice(0,10)),[msg,setMsg]=useState("");const load=()=>api<MileageReport[]>(`/reports/mileage?startDate=${start}&endDate=${end}`).then(setRows).catch(e=>setMsg(e.message));useEffect(()=>{void load();},[]);const approved=rows.filter(x=>x.status==="Approved");return <><div className="stats"><div className="card"><span>行程</span><b>{rows.length}</b></div><div className="card"><span>已核准</span><b>{approved.length}</b></div><div className="card"><span>核定里程</span><b>{approved.reduce((s,x)=>s+(x.approvedDistanceKm||0),0).toFixed(1)} km</b></div><div className="card"><span>補助</span><b>{money(approved.reduce((s,x)=>s+(x.approvedAmount||0),0))}</b></div></div><div className="card"><div className="head"><h2>整體報表｜唯讀</h2><div className="actions"><button className="btn secondary" onClick={()=>downloadCsv("督導整體報表.csv",rows as unknown as Record<string,unknown>[])}>下載</button><button className="btn" onClick={load}>查詢</button></div></div><div className="grid two"><label>開始<input type="date" value={start} onChange={e=>setStart(e.target.value)}/></label><label>結束<input type="date" value={end} onChange={e=>setEnd(e.target.value)}/></label></div>{msg&&<div className="alert danger">{msg}</div>}<div className="table"><table><thead><tr><th>日期</th><th>外訪員</th><th>小組</th><th>路線</th><th>核定里程</th><th>補助</th><th>狀態</th></tr></thead><tbody>{rows.map(x=><tr key={x.tripNo}><td>{x.visitDate}</td><td>{x.visitorName}</td><td>{x.teamName}</td><td>{x.route}</td><td>{x.approvedDistanceKm??"—"}</td><td>{money(x.approvedAmount)}</td><td>{x.statusName}</td></tr>)}</tbody></table></div></div></>}
